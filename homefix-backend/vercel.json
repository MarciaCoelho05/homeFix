{
  "version": 2,
  "buildCommand": "npm install && npx prisma generate --schema=./prisma/schema.prisma",
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.js"
    }
  ]
}
